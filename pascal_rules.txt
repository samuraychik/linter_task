*KEYWORDS
begin end if then else while for to do program var function procedure mod div and or not integer char word real boolean array 

*BINARY_OPS
:= + - * / mod div = > < <> >= <= and or

*UNARY_OPS
- not

*SEPARATORS
,

*WHITESPACE_RULESET
BEFORE_BINOP:1
AFTER_BINOP:1
AFTER_UNOP:0
BEFORE_SEP:0
AFTER_SEP:1

*EMPTYLINE_RULESET

*NAMING_RULESET
KEYWORD:UPPER_SNAKE_CASE
IDENTIFIER:PascalCase
