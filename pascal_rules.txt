*KEYWORDS
begin end if then else while for to do program var function procedure mod div and or not integer char word real boolean array 

*BINARY_OPS
:= + - * / mod div = > < <> >= <= and or

*SEPARATORS
, :

*WHITESPACE_RULESET
BEFORE_BINOP:1
AFTER_BINOP:1
BEFORE_SEP:0
AFTER_SEP:1

*EMPTYLINE_RULESET
MAX_IN_A_ROW:2
BETWEEN_SUBROUTINES:1
END_OF_FILE:1

*NAMING_RULESET
IDENTIFIER:UPPER_SNAKE_CASE
